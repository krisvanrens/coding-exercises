name: cpp-raycasting
on:
  push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Cmake build
        #working-directory: ./cpp-fundamentals-exercises/raycasting
        run: |
          ls -lR
          mkdir build
          cd build
          cmake ..
          make
